<div class="row justify-content-center mb-4">
  <div class="col-md-10">
    <div class="card table-card shadow mb-6 rounded-5">
      <div class="card-body py-3 px-3">
        <h4 class="pt-1">
          <i class="fas fa-table mx-2"></i>Master Data <i class="fa fa-angle-double-right  me-2"></i>
          <%- icon %> <%= title %>
        </h4>
        <div class="justify-content-end d-flex">
          <a href="#!" class="btn btn-warning btn-sm mb-3"><i class="fas fa-plus me-1"></i> Tambah
            Data</a>
        </div>

        <div class="table-responsive">

          <table class="table  my-5" id="dataTable">
            <thead class="bg-info text-center">
              <th>No.</th>
              <th>ID Tiket</th>
              <th>Jenis Tiket</th>
              <th>Stok</th>
              <th>Aksi</th>
            </thead>
            <tbody class="text-center">
              <% data.sort().forEach((dt, i) => { %>
              <tr>
                <td><%= (i+1) %></td>
                <td><%= dt.tiket_id %></td>
                <td class="text-capitalize"><%= dt.Jenis_tiket.nama %></td>
                <td><%= dt.stok %> tiket </td>
                <td>
                  <a href="#!" class="btn btn-info btn-sm">
                    <i class="fas fa-edit"></i> Edit
                  </a>
                  <a href="#!" class="btn btn-danger btn-sm">
                    <i class="fas fa-trash"></i> Hapus
                  </a>
                </td>
              </tr>
              <% }) %>
            </tbody>
          </table>

        </div>
      </div>

      <div class="text-center mb-3">
        <a href="/api/home/dashboard" class="btn btn-secondary ms-auto"><i class="fa fa-tachometer-alt me-2"></i>Kembali ke
          Dashboard</a>

        <button onclick="window.history.back()" class="btn btn-secondary">
          <i class="fas fa-backward me-2"></i>Back
        </button>
      </div>
    </div>
  </div>